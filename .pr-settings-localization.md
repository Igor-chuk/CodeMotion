## Summary

- Fix missing localization in the Extensions and GitHub settings tabs.
- Localize the GitHub token save button in all supported languages.
- Replace hardcoded English labels in sidebar, terminal, and file-tab settings.

## Verification

- `npm test`
- `npm run build`
- Verified all settings localization keys in `en`, `ru`, `uk`, `be`, and `pl`.
